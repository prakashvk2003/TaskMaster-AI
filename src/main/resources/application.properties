spring.application.name=taskmaster


spring.ai.ollama.base-url = http://localhost:11434/
spring.ai.ollama.chat.options.model = gemma3:4b

spring.data.mongodb.uri=mongodb://localhost:27017/taskmaster


# Orchestrator Scheduling Configuration
# ISO-8601 duration format (e.g., PT15M = 15 minutes, PT1H = 1 hour, P1D = 1 day)
taskmaster.scheduling.delay=PT1H
# taskmaster.execution.delay=PT5M # Delay for checking tasks ready for execution (if enabled)
# taskmaster.cleanup.cron=0 0 3 * * * # Cron for daily cleanup (if enabled)